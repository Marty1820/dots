#!/bin/sh

symbol() {
    [ "$(cat /sys/class/net/w*/operstate)" = "down" ] && echo "󰤭" || echo "󰤨"
}

name() {
  nmcli -t -f NAME,DEVICE connection show --active \
    | awk -F: '$2=="wlan0"{print $1; exit}'
}

case "$1" in
    icon) symbol ;;
    name) name ;;
esac
